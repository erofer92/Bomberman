package codes;

import java.awt.Point;
import javax.swing.ImageIcon;

public class Player extends Character{
	
	Player(Point p, ImageIcon initialImage, String name){
		super(p, initialImage, name);
	}
	/*
	 * Essa classe precisa de mais alguns métodos especiais.
	 * Decidirei depois.
	 * 
	 */
	
}